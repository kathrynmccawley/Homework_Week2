let rolled = Math.ceil(Math.random() * 6);

// do {
//   console.log("The number is " + rolled);
//   rolled++;
// } while (rolled < 3);

while (rolled < 3) {
  console.log("The number is " + rolled);
  rolled++;
}
